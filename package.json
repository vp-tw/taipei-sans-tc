{
  "name": "taipei-sans-tc-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter doc dev",
    "build": "pnpm build:fonts && pnpm build:site",
    "build:site": "pnpm --filter doc build",
    "build:fonts": "pnpm --filter @vp-tw/taipei-sans-tc fonts:build",
    "preview": "pnpm --filter doc preview",
    "check": "vp check",
    "lint": "oxlint .",
    "format": "oxfmt --check .changeset .github scripts package.json pnpm-workspace.yaml packages/core/package.json packages/doc/astro.config.ts packages/doc/src vite.config.ts",
    "format:write": "oxfmt .changeset .github scripts package.json pnpm-workspace.yaml packages/core/package.json packages/doc/astro.config.ts packages/doc/src vite.config.ts",
    "typecheck": "pnpm --filter doc check && tsc --project tsconfig.json --noEmit",
    "changeset": "changeset",
    "release": "changeset version && pnpm install --lockfile-only && pnpm build && changeset publish",
    "release:publish": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.1",
    "oxfmt": "^0.61.0",
    "oxlint": "^1.76.0",
    "typescript": "^7.0.2",
    "vite-plus": "^0.2.6"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@11.17.0"
}
